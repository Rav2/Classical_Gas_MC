import numpy as np
from matplotlib import pyplot as plt
def f(x, A, B):
    return A*((B/x)**12 - 2*(B/x)**6)

X = np.linspace(0.0, 2.0, 1000)
Y = np.array([f(x, 1.0, 1.0) for x in X])

plt.plot(X, Y, "-")