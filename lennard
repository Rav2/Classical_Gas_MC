import numpy as np
from matplotlib import pyplot as plt
from math import exp
def f(x, A, B):
  return 4*A*((B/x)**12 - (B/x)**6)

X = np.linspace(0.1, 3.0, 1000)
Y = [f(x, 0.00001, 1.0) for x in X]

plt.plot(X, Y, "-")
plt.show()

