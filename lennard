import numpy as np
from matplotlib import pyplot as plt
from math import exp
def f(x, A, B):
  return 4*A*((B/x)**12 - (B/x)**6)

X = np.linspace(0.1, 3.0, 1000)
Y = [f(x, 10.0, 1.0) for x in X]

plt.plot(X, Y, "-")
plt.show()

# A = np.array([1,2,3,4])
# #1+4+9+16 = 30
# print(np.sum(A*A))

